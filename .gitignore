# ----------------------------------------------------
# 1. Python環境/ビルド関連
# ----------------------------------------------------
__pycache__/
*.pyc
venv/
.env

# ----------------------------------------------------
# 2. PyInstaller ビルド成果物
# ----------------------------------------------------
# ビルド中の一時ディレクトリ
build/
# 最終的な実行ファイルが格納されるディレクトリ（ビルド後にGitHub Actionsがアップロードする）
dist/
# ビルドログファイル
*.spec
# 💡 ただし、今回のspecファイルはGitHubで管理する必要があるため、上記はコメントアウトするか
# このリストから除外するかを検討する。
# ここでは、*.spec の除外はしないでおく（PyInstaller設定ファイルはコードの一部と見なす）。

# ----------------------------------------------------
# 3. Docker/ローカル実行関連
# ----------------------------------------------------
# Dockerが生成するデータボリューム（Named Volumeなので通常は問題ないが、念のため）
# 💡 ローカルでテスト実行した際に main.py が生成する data/ フォルダ全体を除外
data/
# SQLite DBファイルそのもの
queue_data.db